language: java
jdk:
  - oraclejdk8
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
before_install:
  - sudo apt-get install libstdc++6
  - git clone --depth=1 https://bitbucket.org/shefinstrumentation/instrumentation.git
  - pushd instrumentation
  - mvn clean install
  - popd
  - pushd lib
  - mvn install:install-file -Dfile=LeapJava.jar -DgroupId=com.leapmotion -DartifactId=leapmotion.sdk -Dversion=1.0 -Dpackaging=jar
  - popd
script:
  - mvn test